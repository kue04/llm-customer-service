# 简历可用素材清单

> 生成日期：2026-09-14
> 所有数字来自本机实测，命令与产物见 README「实测数据」一节。**没跑过的项目一律标了「未实测」，面试时不要说成跑过。**
> 简历上原有但未在本项目验证过的数字（2400 份文档 / 18.6 万片段 / Recall@10 76.8%→90.5% / MRR 0.61→0.79）**不要再用**——本项目知识库只有 515 条，量级完全不同。

---

## 一、可直接粘贴的项目描述（STAR 式）

### 1. 混合召回 + 精排检索链路（最推荐放第一条）

**S** 外卖客服场景中，「退款进度」「商家拒绝退款」「退款失败」等意图的向量相似度都在 0.72–0.83，纯向量召回会稳定混淆业务方向。
**T** 需要一条在 515 条中文知识库上既准确、又能在本地 CPU 跑起来的检索链路。
**A** 用 `bge-small-zh-v1.5` 做embedding，FAISS `IndexFlatIP` 做ANN；在向量分之上叠加 `keyword_bonus` 与 `direction_penalty` 构成混合打分 `score = vector_score + keyword_bonus − direction_penalty`；再用 `bge-reranker-base` cross-encoder 做精排 `rerank_score = score + model_score × 0.01`；对高风险意图（私下转账、验证码、食品安全等）用规则 `detect_intent_hint` 补充候选并加权 0.10；结果按答案去重后取 Top3。
**R** 在 12 条检索评测集（limit=10）上 Recall@1 / MRR / NDCG@10 均为 **1.0000**；关掉关键词加权与方向惩罚的纯向量基线为 Recall@1 **0.9167**、MRR **0.9583**，混合打分带来 **+8.3pp Recall@1**。
**诚实补充**：评测集只有 12 条且已饱和，这个 1.0 只能说明「链路跑通」，不能说明泛化能力。

### 2. 证据分级（primary / supporting）与 Context 构造

**S** Top3 证据同时进 prompt 时，模型会把不同业务方向的结论缝合进同一条回答。
**T** 让模型明确知道「以哪条为准、哪些只能补充」。
**A** `utils/rag_context.py` 把 Top1 标为 `primary`，其余标为 `supporting`，并在 prompt 中写死约束：supporting「只能补充流程、凭证、入口等通用信息，不能覆盖主证据的业务意图」。额外加了一层保护——Top1 与 Top2 的 `rerank_score` 差距小于 0.08 时，primary 标记 `close_match`，prompt 提示避免引用辅助证据的不同业务结论。
**R** 90 条固定集上 `mixed_supporting_intent`（串意图）仅 **3 条**；`answer_basis` 字段会把「主证据：X」返回给前端做溯源展示。
**诚实补充**：`used_primary_evidence` 只有 22/90，判定口径偏严，还没优化。

### 3. 高风险场景规则兜底

**S** 用户会问「你能保证全额退款吗」「把商家微信给我，我私下让他退钱」。让 1.5B 模型自己守边界不稳定，且这类错误的代价不对称——说错一次就是资金或隐私风险。
**T** 建立不依赖模型自觉性的硬边界。
**A** `services/reply_rules.py` 两层设计：query 级强制规则（命中「食品安全+承诺词」「验证码/银行卡」「身份证/真实手机号」「站外交易」等组合时，**直接返回写好的拒绝话术，完全不看检索和生成结果**）；intent 级规则（14 个高风险意图进入 `FORCE_REPLY_INTENTS`，用规则话术覆盖模型输出）。
**R** 在 90 + 30 + 4 = 124 条评测中，**高风险词命中 0 次、承诺类词 0 次**；4 条诱导用例 4/4 被正确拒绝。
**诚实补充**：规则覆盖不到的表述会漏，它是兜底网不是分类器。

### 4. 可归因的评测闭环（这个项目最能体现工程成熟度的一条）

**S** RAG 系统坏了，最常见的问题是「不知道该改哪一层」。
**T** 把「一次回答」拆成可定位的多层信号。
**A** 每个 case 落盘 query / retrieved_items / final_prompt / reply / judge 结果 / 诊断字段；`analyze_grounding_report.py` 输出 `issue_type`、`suggested_layer` 和三类失败归因（`retrieval_failure` / `evidence_insufficient` / `generation_not_using_evidence`），并给出每层对应的修复动作。
**R** 90 条固定集归因结果：pass 75 / retrieval_failure 8 / generation_not_using_evidence 7，另有 5 条判定为 judge 偏严（**不为了分数改业务链路**，单独分流人工复核）。盲测 30 条：pass 23 / retrieval_failure 4 / generation_not_using_evidence 3。
**为什么这么做**：归因表直接决定了下一个迭代改什么，避免「分数低就调 prompt」的盲目试错。

### 5. LLM-as-judge 与 judge 校准

**S** 124 条人工标注成本太高，但 judge 本身也会判错。
**T** 用本地小模型做可复现的自动评分，同时把 judge 的误判单独隔离。
**A** 复用本地 Qwen 作 judge，输出 JSON（`direct_answer` / `grounded` / `useful` / `risk_notes` / `reason`）；解析层处理空响应、非 JSON、缺字段、非法枚举、空 reason 五类失败并分别记录；针对 20+ 类已知误判场景写 `should_relax_*_judgment` 校准规则（如退款时间、验证码话术、赔付边界）。
**R** 124 条评测中 judge 调用成功率 **100%**（124/124），无解析失败；固定集 `evidence_keyword_coverage` **0.837**。
**诚实补充**：judge 和生成用的是同一个 1.5B 模型，存在系统性偏差，没做与外部模型的一致性校验。

### 6. 知识库运营闭环（草稿→审核→发布→回滚）

**S** 真实客服知识库不是静态文件，改一条知识要能影响检索、还能撤回。
**T** 把「改知识」变成有审计、可回滚的操作。
**A** SQLite 存多版本知识条目（draft/pending_review/approved/published/archived）；发布时把 approved 条目追加进知识库 JSONL，**先备份再写入**，然后重建 FAISS 索引、清运行时缓存、记录发布历史；失败自动回滚文件与索引；`rollback_latest_publish` 从备份恢复并回滚状态。FAISS 加载前有四重一致性校验（索引条数、向量维度、文档签名）。
**R** 完整闭环可用，发布/回滚都有历史记录与备份路径。
**诚实补充**：发布与回滚的效果只有功能测试覆盖（`test_knowledge_ops.py` 10 个用例），**没有做过「改一条知识后检索指标如何变化」的量化验证**。

### 7. 工程化与可观测性

**S** 面试项目最怕「跑不起来」和「说不清一次请求发生了什么」。
**T** 让任何人 60 秒验证，让每次请求可追溯。
**A** 拆出 `requirements-dev.txt`（9 个包、不装 torch 也能跑全套测试，因为项目对重型依赖都做了 `try/except` 降级）；CI 跑 ruff + pytest + 单文件体积守护；`/chat/prompt` 返回 `request_id`、`full_trace`（14 个步骤，每步带耗时）、`decision_trace`、`tool_results`、`memory_snapshot`、`evidence_citations`；日志用 `mask_sensitive_text` 脱敏；链路三段降级（检索失败→fallback prompt，生成失败→固定安全回复，规则失败→保留原回复）。
**R** **242 个测试用例 100% 通过**（精简依赖 4.5s）；端到端 P50 **4220ms**、P95 **6147ms**（CPU 推理，90 条样本）；124 条评测中降级触发 **0 次**。
**诚实补充**：没有做过并发压测，QPS 未实测。

---

## 二、面试官可能追问的 10 个问题 + 建议答法

> 每个答案都必须能对上代码位置。答不上来时说「这块我没做到」比编造好。

**Q1：完整链路走一遍，从请求进来到响应出去。**
按 `services/chat_service.py:get_answer_from_rag` 的顺序答：`request_received` → 加载会话与长期记忆 → `analyze_intents` 意图与风险预检 → 订单/退款工具 → 混合召回（FAISS + keyword_bonus − direction_penalty）→ 意图提示补充 → cross-encoder 精排 → `build_prompt_context_items` 证据分级 → `create_prompt` → 本地 Qwen 生成 → `answer_composer` 三段式渲染 → `reply_rules` 规则兜底 → `safety_guard` → grounding 诊断 → 更新记忆 → 落库并返回。14 步都在 `full_trace` 里带耗时。

**Q2：为什么要用混合召回？纯向量不行吗？**
因为语义相关性 ≠ 业务意图正确性。实测「取消订单后钱多久退回来」，cross-encoder 给「支付超时取消」的相关性高于「退款进度」，但业务正确答案是「退款进度」。所以加了 `direction_penalty` 对已知方向相反的组合硬扣分。实测 hybrid 比纯向量 Recall@1 高 8.3pp。**要主动补代价**：这是硬编码规则，换领域要重写。

**Q3：rerank 权重 0.01 是不是太小了？怎么定的？**
是网格搜出来的：0.01 / 0.03 / 0.05 三档在 12 条集上对比，0.03 和 0.05 都会把同一个退款/取消 case 带偏，0.01 最稳。cross-encoder 输出的是未归一化的 logits，量纲和 0–1 的向量分不在一个尺度，所以权重必须小。**补一句**：权重小也意味着 reranker 在当前链路里影响有限——实测 12 条上 rerank 改变 Top1 的次数是 **0**。

**Q4：primary / supporting 分级怎么实现？怎么证明有效？**
`utils/rag_context.py`：Top1 为 primary，其余为 supporting，prompt 里写死 supporting 的约束；Top1 与 Top2 分差 < 0.08 时额外标 `close_match`。有效性证据是 `mixed_supporting_intent` 只有 3/90。**要主动说不足**：`used_primary_evidence` 只有 22/90，说明判定口径偏严或大量回答确实没用上主证据原文，还没查清楚。

**Q5：安全约束写进 prompt 不行吗？为什么一定要规则？**
不行。prompt 是软约束，模型输出有随机性；而且这类错误代价不对称——一次「可以私下转账」就是资金风险。规则是硬约束，命中即返回，不看检索也不看生成。实测 124 条里 forbidden 词命中 0。**补代价**：规则只能覆盖已知表述，错别字变体（「骑首」「验正码」）会漏，盲测集里就抓到了这类 case。

**Q6：LLM-as-judge 靠谱吗？judge 判错了怎么办？**
不把它当标准答案，当初筛工具。解析层处理五类失败（空响应/非 JSON/缺字段/非法枚举/空 reason），judge 判错的情况由 `suggested_layer: judge` 单独分流——**不改业务链路**，只校准 judge 或人工复核。固定集里有 5 条属于这一类。

**Q7：固定集 0.8333 和盲测集 0.7667，差 6.7pp 说明什么？**
说明固定集已经调优过多轮、有过拟合风险，盲测集才是真实参考。两个集都要报，只报固定集是误导。固定集 top1 意图命中 0.8778 vs 盲测 0.8333，也是同样的结论。这正好引出「下一步是扩评测集，不是继续调固定集」。

**Q8：数据都是合成的，怎么保证贴近真实？**
保证不了，这是明确局限。数据说明写在 `data/dataset_sources.md`：全部自建合成，不含真实平台数据、手机号、订单号。合成数据的价值是让「检索链路 + 评测闭环 + 工程化」这三层可验证，而不是证明业务效果。盲测集刻意加了错别字、口语化、强情绪、混合意图来提高难度。

**Q9：P50 4.2s 太慢，怎么优化？**
大头是本地 1.5B 模型生成 256 个 token（CPU）。可选：GPU / 量化 / 换更小模型 / 限制 max_new_tokens / 对高频问题加缓存（项目里已经有 `redis_context_cache.py` 的可选 Redis 层，未启用）；检索侧在 515 条库上是毫秒级，不是瓶颈。**注意**：优化前要先分清是生成慢还是检索慢——`full_trace` 每步都有耗时，这是做性能归因的前提。

**Q10（杀手题）：最终回复和模型原始输出不一样，大模型在你系统里到底起什么作用？**
诚实回答：`compose_answer_if_needed()` **无条件返回组合结果**，实测 15 条样本里只有 1 条最终回复与模型原始输出一致，90 条固定集 `answer_composer_applied = 90/90`。所以默认配置下，最终回复由「主证据 + 规则模板」决定，大模型生成被覆盖。
它的正面价值是：composer 确实在救退化输出（实测有模型只吐 10 个字，被救成 97 字完整回答），并且让 grounding 指标非常稳定。
但这是**当前最大的架构债**，不是好设计。正确的做法是让 composer 只在模型输出低质量时介入——`reply_needs_composer()` 已经实现了这个判断（空回复 / 过短 / 复读 query / 泛化话术 / 与主证据无重叠），但返回值没被用来做分支。下一步是接上这个判断，并做一组 composer on/off 的 A/B 评测，量化规则和模型各自的贡献。
**答出「我知道这是债，以及怎么还」比辩解强得多。**

---

## 三、本项目的 3 个薄弱点 + 被问到时怎么回答

### 薄弱点 1：默认链路里大模型贡献有限，回答基本是「检索 + 模板」

**为什么是薄弱点**：简历写「RAG 系统」，但生成这一环实际被规则覆盖，面试官一追问就会露馅。

**怎么回答**：
> 「这是我在稳定性和可靠性之间做的取舍，但我认为取舍做得过头了。背景是本地 1.5B 模型输出不稳定，会出现复读问题、只吐 10 个字、泛化话术，直接给用户不能接受，所以我加了 answer_composer 做兜底。当时的实现写成了无条件覆盖——现在我实测确认了 15 条里 14 条被重写，这确实是设计缺陷，不是我想要的。修复路径很明确：`reply_needs_composer()` 已经实现了低质量判断，接上去做条件介入，然后跑 A/B 看 composer 到底贡献了多少。」

### 薄弱点 2：评测集太小，且规则硬编码，泛化性没有证明

**为什么是薄弱点**：检索评测只有 12 条、Recall@1 已经 1.0，等于没有区分度；`detect_intent_hint` 是 30+ 条 `if`，`keyword_bonus` / `direction_penalty` 是面向已知 bad case 的手工调参。

**怎么回答**：
> 「我把可控性放在了泛化性前面，代价就是换领域要重写规则。我知道这不是可长期维护的方案，替代路径是用一个轻量意图分类器替换部分规则，或者把方向惩罚改成可学习的。评测集也一样——12 条是我在项目早期为了快速迭代建的，现在已经饱和，扩到 100–300 条并人工标注是我排在第一位的欠账。盲测集 30 条（0.7667）才是我认为的真实水平参考。」

### 薄弱点 3：judge 与生成同一个模型，且多项指标未实测

**为什么是薄弱点**：自己给自己打分有系统性偏差；并发压测、在线模型路径、LoRA 增量、真实对抗集拦截率都没跑过。

**怎么回答**：
> 「judge 我定位成初筛工具而不是标准答案，判错的 case 会单独归到 `suggested_layer: judge` 分流，不为了分数改业务链路。但和更强模型的一致性校验我没做，这是个真实缺口。并发压测、在线模型（需要 API Key）、LoRA adapter 的增量效果我也都没测过——README 里这些项我都标了『未实测』。如果岗位对性能有要求，这是我上手后第一件会补的事。」

---

## 四、一句话电梯陈述（备用）

> 这是一个外卖客服场景的 RAG 后端：515 条中文知识库，混合召回加 cross-encoder 精排，把证据分成主证据和辅助证据防止模型串意图，高风险场景用规则硬兜底，最后用 LLM-as-judge 做可归因的自动评测。124 条评测里高风险词零命中，242 个测试全绿。目前最大的问题是回答主要由规则模板生成、大模型贡献有限，以及评测集太小只有 12 条——这两个都是我已经定位清楚、正在改的债。
