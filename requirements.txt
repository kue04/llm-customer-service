fastapi==0.136.0
uvicorn==0.46.0
transformers==5.6.1
torch==2.11.0
pydantic==2.13.3
datasets==4.8.4
peft==0.19.1
trl==1.2.0
accelerate==1.13.0
sentence-transformers==5.4.1
faiss-cpu==1.13.2
pytest==9.1.1
# 数据接入 / 鉴权改造（RAG_EXECUTION_PLAN）新增：
# 元数据与迁移、JWT 校验
sqlalchemy==2.0.54
alembic==1.20.0
PyJWT==2.14.0
# 文档解析（阶段 2.1 / 2.2）：
# pymupdf 是 PDF 解析与「整页渲染送 OCR」的唯一依赖，带预编译 wheel，无需本地编译；
# python-docx 读 DOCX；beautifulsoup4 走 HTML 结构遍历；trafilatura 是 HTML 正文兜底抽取。
# 这四个包已在 requirements-dev.txt 里同样列出：解析器测试没有 skip 分支，
# 缺任何一个都会让 CI 直接红掉，因此不能只放在「完整安装」这一侧。
pymupdf==1.28.2
python-docx==1.2.0
beautifulsoup4==4.15.0
trafilatura==2.2.0
# 扫描件 OCR：默认不装（会拖进 onnxruntime 与模型权重）。
# 需要时执行 pip install rapidocr-onnxruntime 即可生效，代码无需改动（见 parsers/ocr.py）。
# PostgreSQL 驱动：本机不装也能跑（默认 SQLite），
# 部署到 PostgreSQL 时取消下面一行的注释并按需改版本
# psycopg[binary]==3.2.3
