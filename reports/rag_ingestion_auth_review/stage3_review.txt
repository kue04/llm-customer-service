阶段编号：3（范围：3.1 / 3.2 / 3.3 / 3.4 **全部完成**；阶段级结论**已回填** → 见第五节）
阶段目标：Chunk 切分、持久化与索引（3.1 Chunk 配置 / 3.2 结构化切分器 /
         3.3 幂等流水线 + worker / 3.4 FAISS manifest 与原子切换）
审查日期：2026-09-23

★ 范围声明（必须先读）★
────────────────────────────────────────────────────────────────────
本文件在批次 B5（任务 3.1 + 3.2）结束时**创建**，但**阶段 3 尚未完成**：
3.3（幂等流水线 + worker）与 3.4（FAISS manifest / 原子切换）都还没开始。

因此本文：
  · 只记录 3.1 / 3.2 的完成情况与风险；
  · **不给出阶段结论，不写 PASS**（阶段级结论一栏显式写「待回填」）。

为什么不提前写 PASS：阶段 3 真正交付的是「上传 → 切分 → 落库 → 进索引 → 可检索」
这条闭环，而 3.1 / 3.2 只交付了其中的**切分**环节 —— chunk 目前还没有任何生产调用点。
此时若写 PASS，下一个窗口会把「已 PASS」读成「这一段可以不用回头看了」，
而恰恰**接线（3.3 / 3.4）才是最容易出问题的地方**。
（`stage2_review.txt` 开头保留了一段同类教训的修订说明，可对照阅读：
上一版是在「只差同一阶段的剩余任务」时不给 NEEDS_WORK，避免自锁；
本处是反向情形 —— 阶段还有两个未开始的任务，不能给 PASS。）

═══ 一、自动检查结果（门禁） ═══

证据文件（reports/rag_ingestion_auth_review/）：
  B5_full_test-output.txt        pytest -q → 790 passed, 5 warnings, 4 subtests passed
  B5_task3_2_test-output.txt     pytest tests/test_chunking.py -q → 235 passed
  B5_ruff-output.txt             ruff check . → All checks passed!
  B5_compileall-output.txt       compileall → 退出码 0
  B5_data-size-output.txt        check_repo_data_size.py → 通过，无超标文件

起点 / 终点对比（基线纪律）：
  B3 收尾 448 → B4 收尾 **555**（本批起点）→ B5 收尾 **790**，本批新增 235 条，零回归。
  warning：起点 5 条 → 终点 5 条，逐条核对均为既有噪声
  （fastapi[1] + starlette[1] 的 httpx 弃用提示、faiss SWIG 绑定[3]），**未新增**。

范围核对（确认没有越界改到别的批次）：
  services/ingestion/parsers/ 与 utils/vector_retriever.py **零改动**
  （git status 无输出；routers/documents.py 的未跟踪状态是 B4 遗留，非本批改动）
  生产代码路径对 chunkers 的引用：仅 services/ingestion/__init__.py 的模块登记，
  **没有任何业务调用点**（B6 的 pipeline 才是第一个调用方）—— 见第四节遗留问题 1。

═══ 二、人工审查结果（任务级，逐条对照计划原文） ═══

完整逐条核对见同目录 **B5_task_level_review.txt**（本文件不重复粘贴）。
要点摘要：

3.1 定义 Chunk 配置
  · 六个默认值与计划逐值一致；越界 / 类型错 / 未知键 / 布尔字段写成字符串
    一律抛 `ChunkConfigError`，错误对象含逐字段 (field, message, value)；
  · 模块级 `CHUNK_CONFIG` 在 **import 期**构造并校验 → 非法配置让进程启动失败
    （有子进程用例证明 stderr 里能看到字段名）；
  · `storage_payload()` 提供「生效配置 + tokenizer 标识 + config_version」，
    可直接落 `document_versions.metadata_json["chunking"]`；
  · 计划外新增第 7 个配置项 `parent_max_tokens`（默认 2800），理由：给超大 section
    的 parent 设上界，避免产出一条几十万字符、既不被 embedding 也不被引用的记录。

3.2 结构化切分器（计划 9 条要求逐条有实现 + 精确断言）
  1 标题层级 + 页码分组（同名标题两处不合并） 2 超长段落四级拆分（句末标点 → 分号 →
  换行 → 按 token 硬切） 3 表格按行分块且每组重复表头；代码块不内部截断
  4 列表项按项边界装箱，仅单项超限才项内拆分并计数 5 相邻 child 重叠 ≥80 tokens、
  每块 ≤700 tokens 6 每个 child 指向同组 parent、父先于子 7 标题路径作前缀且三重判据
  防重复 8 空白 / 页码行 / 重复页眉页脚 / 完全重复段落四道清洗且全部计数
  9 12 个字段齐备 + 归属信息能整体进 metadata_json（真库读回验证）

  测试覆盖（计划点名的九项全中）：长段落 / 表格 / 代码 / 列表 / 重叠 / 边界长度 /
  空文档 / 确定性 ID / 权限元数据继承；**tokenizer 用固定 mock，测试不下载模型**
  （另有一组用例用生产默认配置 + 默认启发式 tokenizer 复跑，防止「mock 下全绿、
  真实配置一跑就炸」）。

  枚举型契约用真值表而非抽样：块类型全集（8 种 → 产出几个 chunk）、ACL 权限（5）与
  主体类型（4）、ChunkingStats 字段全集（17）、tokenizer 注册表。

「超出计划原文」的 12 处取舍已逐条标方向（更严 / 更宽 / 更窄 / 补全 / 计划外新增），
见 B5_task_level_review.txt 第四节。其中需要特别注意的一条：

  **代码块超 `max_tokens` 时整块保留（标 oversize 并计数）**——
  计划 3.2 第 3 条（代码不内部截断）与第 5 条（不超 700 tokens）在这一点上直接冲突，
  实现选择了「不损坏内容 + 可观测」，属**有意放宽硬上限**。
  这个例外只对代码块成立，正文段落仍受硬约束；不得顺手扩大到其它块类型。

回头看 B4：
  · [正面确认] B4 的 `insert_chunks` 声称「父必须先于子写入」—— 本批用真库
    （SQLite + PRAGMA foreign_keys=ON + alembic head）验证该约束确实由数据库强制，
    B4 的说法成立。
  · [新发现，登记不改] `repository.list_chunks` 排序键为 (created_at, chunk_id)，
    同批写入的 created_at 可能同值 → **读回顺序 ≠ 写入顺序**。
    B6 需要文档顺序时必须读 `metadata_json["ordinal"]`。不属本批范围，不修 B4 代码。

═══ 三、风险与回滚点 ═══

1. **本批不接任何生产链路**，风险面仅限新增文件 + `services/ingestion/__init__.py`
   的一行导出登记。回滚方式：删除
   `config/chunking_config.py`、`services/ingestion/chunkers/`、`tests/test_chunking.py`，
   还原 `services/ingestion/__init__.py`（去掉 chunkers 的登记）。无数据迁移、无表结构变更。
2. **无新增依赖**（纯 stdlib + 项目内模块），因此不存在「依赖装不上导致 CI 红」的风险。
3. **未新增 Alembic 迁移**：本批不写库、不改表，0001 仍是最新迁移。
4. 主要风险集中在 **B6 接线**（下一批），不在本批：
   · chunk 的读回顺序不稳定（依赖 ordinal → 见上）；
   · `parent_chunk_enabled=False` 时 child 的 parent_chunk_id 为 NULL，
     B6 的插入/检索逻辑要能处理这一形态（本批已用测试固定该形态）；
   · 换真实 tokenizer 会改变 chunk_id → 必须重建索引（设计预期，不是缺陷）。

═══ 四、遗留问题 ═══

1. **chunker 尚无生产调用点**（有意为之：B6 的 pipeline 是第一个调用方）。
   按项目纪律「每个新模块必须登记生产调用点」，已把 `chunk_document()` 登记进
   台账第 4 节的「已建未启用」清单，避免被误读为「闭环已完成」。
2. **「知识库配置覆盖」只交付了机制**（mapping → 校验后的配置对象映射）。
   从 `knowledge_bases.chunking_config_json` 读出来、并把生效配置写进
   `document_versions.metadata_json["chunking"]` 的动作属于 B6 流水线。
3. 图片块（`image_ref`）不进 chunk —— 本批切分器只处理可检索文本，
   图文混合检索需单独设计。
4. PDF 表格「跨页重复表头」未覆盖：本批的重复表头只在**同一个表格块内**成立，
   跨页拆分的表格需要解析器侧配合（属 B3 范围，不在本批）。
5. `list_chunks` 读回顺序 ≠ 写入顺序（第二节回头看 B4 第 2 条）。
6. token 数是确定性启发式估计，与真实 BPE 有偏差；真实引擎 / PG / Redis / OCR 的
   复验仍归 B6 / B8 门禁（与 B1~B4 相同的环境限制：本机无 Docker / psql）。
7. 检索评测口径失真（F2）与检索层无隔离测试（F3）不在本批范围，仍待 B7 与
   独立小批次处理；本批未触碰任何评测脚本。

═══ 五、阶段结论 ═══

【回填记录 2026-09-23（B6 批次）】
────────────────────────────────────────────────────────────────────
本节原文是「**待回填** —— 阶段 3 只完成 3.1 / 3.2」，并写明
「本文件将在阶段 3 的**最后一个批次**（B6）完成后回填阶段级结论」。

B6 已于 2026-09-23 完成（任务 3.3 幂等流水线 + worker、任务 3.4 FAISS manifest 与原子切换），
**阶段 3 的四个任务全部完成**，因此按当时的约定回填本节。

下面保留原文以便对照（**历史不可删**），结论区在其后。

  ── 原文（2026-09-23，B5 批次）──
  **待回填** —— 阶段 3 只完成 3.1 / 3.2；3.3（幂等流水线 + worker）与
  3.4（FAISS manifest + 原子切换）尚未开始。
  本文件将在阶段 3 的**最后一个批次**（B6）完成后回填阶段级结论。
  **在此之前不得引用本文件作为「阶段 3 已通过」的依据。**
  ── 原文结束 ──

★★★ 阶段级结论（回填后，2026-09-23）：**PASS** ★★★

判据（四条同时成立）：

1. **范围完整**：阶段 3 的四个任务全部有实现 + 精确断言 ——
   3.1 Chunk 配置 / 3.2 结构化切分器（B5，`B5_task_level_review.txt`）、
   3.3 幂等流水线 + worker / 3.4 FAISS manifest 与原子切换
   （B6，`B6_task_level_review.txt`）。**没有"还剩一个任务"的情况。**
2. **两个批次的任务级结论都是 PASS**（B5、B6），且 B6 是本阶段最后一批。
3. **门禁全绿且零回归**：阶段 3 起点 555（B4 收尾）→ 790（B5 收尾）→ **839（B6 收尾）**，
   `ruff check .` 干净、`compileall` 退出码 0、`check_repo_data_size.py` 通过、
   warning 5 条且逐条为既有噪声（未新增）。
4. **本阶段交付的那条闭环真的接上了** —— 这是当初拒绝提前给 PASS 的**唯一理由**，
   现在该理由已消失：B5 时「chunk 还没有任何生产调用点」，B6 后
   `chunk_document()` / `insert_chunks()` / 队列消费端 / `index_builds.manifest_uri`
   四行**全部从「已建未启用」清单里消失**，并有 AST 守卫测试守着
   （`TestProductionCallPoints`）。「上传 → 解析 → 切分 → 落库 → 建索引 → 可检索」
   已是一条真实可跑的链路。

阶段 3 期间修掉的**真缺陷**（不是环境噪声）：

  · B6 基线暴露：`delete_chunks` 逐行 ORM 删除撞复合自引用外键，
    导致"任务成功后重复投递"必失败（异常类型 `IntegrityError`，抛出点在项目自己的 SQL）。
    已修（改为单语句批量 DELETE）并留证：`B6_baseline_test.txt`（问题现场）→
    `B6_full_test-output.txt`（修复后 839 passed）。踩坑编号 **B13**。

★ 与 B5 那次的纪律对照（为什么当初不给 PASS 是对的）
  `stage2_review.txt` 开头保留的教训是"只差同一阶段的剩余任务时不给 NEEDS_WORK，避免自锁"；
  本次是**反向情形**：阶段还有两个未开始的任务，且它们正是**最容易出问题的接线部分**
  （事实也验证了 —— B6 确实在接线路径上炸出了唯一那条真缺陷）。
  若 B5 时写了 PASS，这个缺陷会以"已通过"的姿态进入下一个窗口，很可能被当成环境噪声放过。

═══ 六、遗留问题在阶段 3 收尾时的状态 ═══

第五节（上文）列出的 7 条 B5 遗留问题，在阶段 3 收尾时的实际去向：

  | # | B5 遗留问题 | 阶段 3 收尾状态 |
  | --- | --- | --- |
  | 1 | chunker 尚无生产调用点 | ✅ **已消除**（B6 接线，pipeline.py 是第一个调用方） |
  | 2 | 知识库配置覆盖只交了机制 | ✅ **已消除**（B6 流水线读出 + 落库，越界失败而非夹取） |
  | 3 | 图片块不进 chunk | ⏳ 仍然存在（设计如此：图文混合检索需单独设计） |
  | 4 | PDF 跨页重复表头未覆盖 | ⏳ 仍然存在（属 B3 解析器范围） |
  | 5 | `list_chunks` 读回顺序 ≠ 写入顺序 | ✅ **已规避**（B6 索引侧按 `ordinal` 排序并留痕） |
  | 6 | token 数偏差 / 真实引擎未复验 | ⏳ 仍然存在（→ B8 门禁，本机无 Docker） |
  | 7 | 评测口径失真（F2）与检索层无隔离测试（F3） | ⏳ **部分推进**：B6 交付了带强制 filter 的 chunk 级检索路径，但端到端隔离测试仍缺 → B7 |

B6 新增的 7 条遗留问题见 `B6_task_level_review.txt` 第九节，
其中**不阻断阶段 3 收尾**（软件发布门禁与后续批次接线）。

（说明：第五节原文末行的「本批的任务级结论：PASS」指的是 B5 那批，不是阶段结论。）

────────────────────────────────────────────────────────────────────
审查人：AI（B5 批次创建 / B6 批次回填）
审查日期：2026-09-23
