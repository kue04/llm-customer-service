任务级审查：验收规范落盘与现状对照（v2.0 → v2.1）
审查日期：2026-09-23
审查人：AI（规范落盘任务）
审查范围：企业级验收规范的仓库化 + 与项目现状的对齐修正
          源：C:\Users\kk\Downloads\RAG系统方案与企业级验收规范.md（v2.0，**未改动**，留作对照）
          目标：docs/RAG_ENTERPRISE_ACCEPTANCE_SPEC.md（v2.1，仓库内稳定引用路径）
依据：docs/goal.md；docs/RAG_NEXT_WINDOW_PROMPT.md（2026-09-23 基线校正）；
      docs/RAG_GAP_ANALYSIS_AND_ROADMAP.md 第 1/9 节；docs/RAG_EXECUTION_PROGRESS.md 第 1/2 节；
      reports/rag_ingestion_auth_review/stage1~4_review.txt
说明：本次**不是批次任务、不是阶段任务**，是「规范落盘」这一件独立工作。
      因此本文件不出具阶段结论，只出具任务级结论；它也**不改变 B8 的排期**。

────────────────────────────────────────────────────────────────────
一句话结论
────────────────────────────────────────────────────────────────────
v2.0 的问题不是「标准定高了」，而是**「目标」和「现状」混在一格写**：
它自称非审计报告，却想当验收基准，结果两边都用不上 —— 当设计参考，有 3 处事实已过期；
当验收基准，有 7 处直接和现状冲突。

本次**一个门槛数字都没动**（§10.3 的 28 个指标定义、§10.5 的首轮门槛原文照旧），
只做了三件事：**修正过期事实 → 拆开「目标/现状」 → 把执行层规则单开一节**。
改完的效果是：这份规范同时变成了 `RAG_GAP_ANALYSIS_AND_ROADMAP.md` 的**按指标细化版**，
面试时可以直接讲「我对着企业级验收标准逐条自查过，每条都有已实现/部分/未实现和证据」。

═══ 一、改动清单（16 处，分四类） ═══

【A 类 · 事实已过期，必须改】3 处

  A1+A2  §1 第 11 行「本次未收到源码，因此不能断言现有系统缺失了某项实现」
         → 该前提已失效（源码在手 + 阶段 0~4 已落地并逐阶段审查），
           改为「已知/未掌握」两列表 + 「已实测 / 设计建议」两类判断的引用纪律。
  A3     §1 第 5 行「配套文档《RAG前端增量改造与指标展示规范》」
         → 仓库内无此文件。v2.0 称其「通过阶段 ID、指标 ID、需求 ID 对齐」属不实描述，
           已更正为「本项目尚无此文件」，并说明现有前端材料的性质差异。

【B 类 · 会被误当验收项，加「当前状态 + 证据」】7 处

  B1  §8.1 混合检索      → 未实现（纯稠密单路）。连带写明 §11.1「Dense 故障切 BM25」不可执行。
  B2  §6.2 方法矩阵      → 加「本项目」列 11 行，并明确验收范围是 4/5 种格式而非全表 11 行。
  B3  §7.3 三状态字段    → 未实现（单一 status 枚举）；质量门禁未实现；
                           并加「published ≠ 已过质量门禁」的歧义警示。
  B4  §10.5 门槛表       → 加「本项目可测性」列 9 行（部分可测 4 / 不可测 4 / 部分可测）。
  B5  §5.2 恶意内容防护  → 未实现，8 行控制项逐项标注；写明「越权=0」不等于「恶意文件=0」。
  B6  §2.1 Q01 限流      → 未实现（已说明 grep 命中的是 markdown 解析器的误命中）。
  B7  §11.1/11.2/11.3    → deadline/背压/死信/成本账本 未实现；RPO·RTO 未演练；
                           并把「未经验证的回滚不计入能力」写入正文（D1）。

【C 类 · 项目已付学费、规范里缺的】新增 §17 六节

  §17.1 门禁取数口径   —— 以 JUnit XML 为权威；退出码/汇总行不可作判据（踩坑 A6 的实证）
  §17.2 结论状态机     —— 只有 PASS/NEEDS_WORK；阶段未完成不写 PASS；阶段 PASS ≠ 门禁 PASS；
                          不可测项写「无法判定」，不许写 PASS 也不许写「未达标」
  §17.3 已建未启用判据 —— 定义在、测试在、无人调用 = 未完成（F1/F3 与回滚资产的教训）
  §17.4 证据落盘与 append-only —— 红/绿双份、三类文件只追加、记录必须带日期、踩坑四段式
  §17.5 编号映射表     —— 规范 I01~O01 ↔ 项目 B1~B8/阶段 0~7，并点明两处编号撞车陷阱
  §17.6 与 §10.3/§10.5 的关系 —— 执行层不新增也不修改任何指标与门槛

【D 类 · 自我修正】1 处 + 2 处配套

  D1     §11.3 补「回滚能力必须由测试或演练证明；未经证明的回滚不计入能力，只能记为预留」
  配套1  §10.3 表末补 N/A 三判据（分母为 0 / 标签缺失 / **实现口径与本定义不一致**）
  配套2  §1.0 新增「本项目当前状态总览」13 行（导航用），§13 加「完成证据」列读法提示

═══ 二、事实核验（每条现状声明都可复现） ═══

本次所有「未实现 / 已建未启用」的判断，均在 2026-09-23 用命令实测，未采信记忆或推断：

  混合检索         grep -ri "bm25|sparse" --include=*.py → 零命中
  检索实现         services/ingestion/index_builder.py:348 → faiss.IndexFlatIP（纯稠密）
  重排（对照组）   utils/vector_retriever.py:88 / :664 → CrossEncoder，**已实现**
  OCR              services/ingestion/parsers/ocr.py → 仅协议 + 阈值判定，模块头明示不装引擎
  来源格式         services/ingestion/models.py:46 → 5 种（pdf/docx/html/md/txt）
  质量门禁         grep -ri quality --include=*.py services/ingestion → 零命中
  状态字段         services/ingestion/models.py:76-86 → 单一 DOCUMENT_STATUSES 枚举
  病毒扫描         grep -ri "virus|clamav|malware" → 零命中
  限流             grep -ri "rate_limit|limiter" → 仅 markdown.py 的 _TABLE_DELIMITER_RE（误命中）
  deadline/背压/死信  grep -ri "deadline|backpressure|dead_letter" → 零命中
  索引回滚         6 个符号在 index_builder.py:148/416/503/510/516/87 定义，
                   排除本文件后**各 0 处调用点**
  检索指标口径     scripts/evaluate_retrieval_metrics.py:85-87 → is_relevant() 按 intent 判相关
  测试基线         tmp/b8_baseline_junit.xml → tests=884 failures=0 errors=0 skipped=0

═══ 三、交付前机械校验（8 项，全过） ═══

脚本：scripts/verify_acceptance_spec.py（**已入库**，可复跑）
环境：C:/Users/kk/.workbuddy/binaries/python/envs/default/Scripts/python.exe
命令：<上列 python> scripts/verify_acceptance_spec.py docs/RAG_ENTERPRISE_ACCEPTANCE_SPEC.md

  文档：RAG_ENTERPRISE_ACCEPTANCE_SPEC.md  行数=737
  标题：md=60 html命中=61 | 围栏=4 代码块=2 | 表格 md=24 html=24
  顶层章节=17 小节=43 | 交叉引用=22 个，其中悬空=0
  RESULT: OK —— 8 项校验全部通过

  ① 标题缺失 = 0（60 个 md 标题全部渲染；html 61 = 60 + 头部「v2.1 修订说明」h3）
  ② 围栏 4 为偶数，渲染代码块 2 == 围栏/2
  ③ 代码内容未泄漏到正文
  ④ 表格数一致（md 24 = html 24，含引用块内的 `> |` 表格）
  ⑤ 围栏按 h2 分段全为偶数（无跨章节未闭合）
  ⑥ 顶层章节编号 1~17 连续无缺
  ⑦ 小节号无重复；⑧ 22 个 §X.Y 交叉引用全部指向存在的章节（0 悬空）
  附加：表格列数一致性校验通过

> 该脚本由本次任务新建并入库（`scripts/verify_acceptance_spec.py`），定位是
> 「这份规范每次改动后的回归门禁」——规范改了就跑一次，比人工通读可靠。
> 它自身通过 `ruff check`。

═══ 四、审查中发现的问题与处置（本节是审查的产出，不是走过场） ═══

本次交付前自检出 4 个问题，**全部已修并复跑校验**：

  1【真问题】§17.3 的 bash 代码块写在列表项内、缩进 2 空格
     → 渲染结果被吞成行内代码（`<p><code>bash ...`），**代码块消失**。
     实测 2 空格、4 空格两种缩进均不被识别，只有取消缩进可用 →
     已改为独立代码块并在校验脚本中把该情形纳入检测口径。
     ★ 这是 skill「长文档静默故障」的典型形态：源文件肉眼看完全正常，只有渲染才暴露。

  2【口径不一致】§1.0 的取值说明只列了 2 个受控标注，实际表中用了 3 个（漏「未演练」）
     → 已补齐；并补「部分实现可简写为『部分』」的说明（§6.2 矩阵用了简写）。

  3【措辞错误】§1.0 使用纪律写「不得出现在 §10.5 门槛表的『已达结果』栏」，
     但 §10.5 并没有「已达结果」栏（本次新增的是「本项目可测性」列）→ 已按实际列名改写。

  4【格式规范】§7.3 用了 ⚠️ 符号 → 已改为「注意：」文字（本规范正文不用图形符号）。

  另有一处**因外部变化而修正**：写作过程中仓库新增了
  docs/FRONTEND_HANDOFF_AND_ALIGNMENT.md 与 docs/frontend/FRONTEND_API_CONTRACT.md
  → A3 的措辞从「待编写」改为「本项目尚无此文件，现有前端材料是接口/交接性质，不能顶替」。

═══ 五、结论 ═══

  【任务级结论】**PASS**

  判据：
   · 16 处改动全部落盘，且**门槛数字与指标定义零改动**（可 diff 自证）；
   · 8 项机械校验全过，0 悬空引用，表格/围栏/标题结构完好；
   · §1.0 的 13 条现状声明全部有 2026-09-23 的实测命令或源码行号支撑；
   · 自检发现的 4 个问题全部修复并复跑校验。

  声明范围（避免过度声明）：
   · 本结论**只覆盖「规范落盘与现状对照」这一件事**；
   · **与 B8 的发布门禁无关**，B8 的门禁结论不因本文件而改变；
   · 规范里标 `未实现 / 未就绪 / 已建未启用 / 未演练` 的 13 项，**状态未因本次修订改变**——
     本次只是把现状写清楚，**没有修任何功能**。

═══ 六、遗留与下一步 ═══

  1. §17.5 映射表需**随批次推进就地更新**（该节已写明这是它唯一允许覆盖更新的原因）。
  2. §10.5 标「不可测」的 4 类（输入 / 检索 / 性能 / 成本），要变得可测需先补前置：
     gold 意图与路由标注集、gold 源 span 集、检索口径修正、压测与 P95 聚合、成本账本。
     **这属于 B8 之后的路线，不在 B8 门禁范围内**，不得据此给 B8 记 NEEDS_WORK。
  3. 本次**未覆盖**并行会话产出的前端材料（docs/FRONTEND_HANDOFF_AND_ALIGNMENT.md、
     docs/frontend/*、scripts/{export_frontend_contract,mint_dev_token,seed_dev_tenant}.py）。
     它们不由本任务审查、也未纳入本次提交。
  4. v2.0 原件保留在 C:\Users\kk\Downloads\RAG系统方案与企业级验收规范.md，
     后续如需追溯「原文怎么写的」，以该文件为准。
