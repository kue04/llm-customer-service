# 简历优化清单（2026-09-25）

> **依据**：本机实测 + `docs/RAG_ENTERPRISE_ACCEPTANCE_SPEC.md` §1.0（能力真实状态的唯一口径）
> + `reports/rag_ingestion_auth_review/stage7_review.txt`（发布门禁结论**与它的三条限制**）
> + `README.md` §3.2 / §3.5（指标与性能的权威表）。
> **原则**：每一条都能对上代码或产物；对不上的不写。
>
> ⚠️ 前提：你的仓库是**公开的**（`github.com/kue04`）。面试官可能去看。
> 所以简历**必须与仓库自洽** —— README 里那行 `f1-track: chat-service-retrieval=seed-faq`
> 就是仓库在说「双轨未合」。简历不能写成已经合了。

---

## 零、一句话结论

| 类别 | 数量 | 说明 |
| --- | --- | --- |
| **必修**（会被追问穿帮） | 3 | 「混合召回」这个术语、检索指标的金标口径、技能栏里没用过的技术 |
| **白丢的亮点**（一个字没写） | 3 | 鉴权与多租户、索引版本管理与原子发布、测试规模与「不装模型可跑全套测试」 |
| **数字过时** | 2 | 252 → 936；项目时间 2026.02-2026.06 → 至今 |

**最重要的一条判断**：你简历的问题不是写得少，是**有两个词经不起第二层追问**。
面试官问「BM25 你怎么融合的」和「你的 Recall gold 是谁标的」——这两个问题答不好，
前面所有指标的可信度会一起被打折。修掉它们比加新亮点更优先。

---

## 一、必修三条

### 必修 1：「混合召回」这个词要改（风险：高）

**简历原话**：「混合召回 + 精排检索链路 …… 在 FAISS 向量分之上叠加关键词加权与业务方向惩罚」

**问题**：业界说「混合检索 / hybrid search」默认指 **稠密 + 稀疏（BM25）两路独立召回再做融合**（RRF 或加权）。
而你做的是**单路向量召回 + 规则加权**。规范 §1.0 第 1 行明确记着：

> 混合检索：稠密 + 稀疏(BM25) + RRF 融合 —— **未实现**（纯稠密单路）

**代码级证据**（2026-09-25 逐行核对，不用只信结论）：

| 证据 | 位置 | 说明 |
| --- | --- | --- |
| 召回只有一路 | `utils/vector_retriever.py:764` | `faiss_hits = _search_real_faiss(query, top_k=max(limit*5, 20))`，后续循环**只遍历 faiss_hits** |
| `use_hybrid=True` 不改召回 | 同文件 `:721-723` | `bonus`/`penalty` 是 `final_score = similarity + bonus − penalty` 的**加项**，`similarity` 已由 FAISS 定好 |
| 关键词是硬编码表 | 同文件 `:890-916` | 16 个领域词权重表 + `if keyword in query and keyword in field_text` —— **无分词、无词频、无 IDF、无长度归一化** |
| 全仓没有稀疏路 | `grep -ri "bm25\|sparse"` | 生产代码零命中（仅第三方库噪音） |

**为什么你会记成「关键词检索」**——三条来源，每条都真实存在过，只是都不在生产链路：

1. **确实有一个独立的纯词法检索器**：`utils/retriever.py` 的 `score_knowledge_item` / `retrieve_document_candidates`（`FIELD_WEIGHTS` 字段加权 + 术语重叠，遍历全量 item）。但它**只被 3 个脚本调用**（`scripts/evaluate_retrieval.py`、`debug_prompt.py`、`audit_knowledge_base.py`），
   `routers/` 与 `services/` **零调用** → **评测基线与调试工具，已退役**。
   （`vector_retriever.py:22` 只 import 了它的 `iter_knowledge_items` 与 `is_similar_answer` —— 读数据与去重，**不是打分函数**。）
2. **代码里到处是「关键词」字样**：`calculate_keyword_bonus`、`detect_intent_hint`、`keyword_weights`、满屏 `if "退款" in query`。读代码确实像「有关键词检索」。
3. **`hybrid` 被当成模式名在用**：`routers/retrieval.py:234` `use_hybrid=request.mode == "hybrid"`、
   `scripts/evaluate_retrieval_metrics.py --mode {hybrid,vector}`。
   而它自己的 help 写得最清楚：
   > `hybrid: vector + keyword bonus - direction penalty; vector: embedding only.`

   —— **项目内部对 "hybrid" 的定义就是「加不加 bonus」，不是「两路召回」。** 这个命名会误导每一个读代码的人，包括你，也包括面试官。

**面试官会怎么问**（按杀伤力排序）：
1. 「BM25 用的什么分词？jieba 还是 IK？」→ 你没上 BM25，答不上来就是术语堆砌
2. 「RRF 的 k 取多少？还是加权融合？两路分数怎么归一化到同一尺度？」
3. 「稀疏那路召回率贡献了多少？做过只开稠密 vs 只开稀疏的对比吗？」

**改法**（不用改代码，只改措辞）：

> ~~混合召回 + 精排检索链路~~
> → **向量召回 + 规则补偿的多信号重排**
> → 展开写法：`FAISS 稠密召回 → 意图规则定向补充候选 → 关键词领域加权 + 业务方向惩罚 → cross-encoder 精排`

**你有一条能正当主张的「第二路」，别浪费**：`supplement_candidates_by_intent_hint`
（`utils/vector_retriever.py:624`）—— 意图识别命中时，它**从全量文档里把 `intent` 相符的条目定向补进候选集**
（固定给 `INTENT_HINT_SUPPLEMENT_SCORE = 0.80`，`_retrieval_origin` 标为 `intent_hint_supplement`）。

这**确实是第二个候选来源**，但它由**意图分类规则**驱动，不是词法打分召回。所以准确说法是
「**向量召回 + 意图规则补偿**」，不是「稠密 + 稀疏混合」。
（诚实补充，等着被问：0.80 是硬编码固定分，本质是「定向置顶」而非学分融合 —— 主动说，别被问出来。）

并在面试时**主动划边界**：

> 「我这版召回是 **FAISS 单路稠密向量**，没有 BM25 稀疏路和 RRF 融合。
> 我做的『多路』是**意图规则补偿**：意图识别命中时按 intent 定向补候选，解决
> 『意图正确但字面差别大的条目被向量排出 top-k』这个问题。
> 为什么不上 BM25：语料只有 781 条、同义改写少，领域规则信号比词频信号更可靠；
> 上 BM25 要额外引入分词器与融合权重两套调参，收益不确定。
> 代价我清楚：**规则是为已知 bad case 手工写的，换领域要重写**。」

主动说「我没做 X，因为 Y」比含糊带过值钱得多 —— 它证明你知道 X 是什么。

### 必修 2：检索指标要补口径限定（风险：中高）

**简历原话**：「复用 grounding 用例集自带的 `expected_intent` 作检索金标，**零额外标注**把评测集扩到 90 条固定 + 30 条盲测 …… 固定集 Recall@1 0.9667 / MRR 0.9773 / NDCG@10 0.9827」

**问题**：数字本身是对的（README §3.2 权威表一致）。但「相关」的判定用的是
`candidate["source"]["intent"]` —— **候选自己带的 intent 字段**，不是人工标注的 gold span。
规范 §1.0 第 13 行的定性是：

> 检索指标 M10 Recall@5≥0.85 —— **未就绪**：口径失真 + 无 gold 源 span

**面试官会怎么问**：
1. 「你的 Recall 怎么算的？gold 是谁标的？」→ 答「复用用例自带的 expected_intent」时，
   严谨的面试官会立刻指出：**这是用同一套意图判定给自己打分，属自证**
2. 「M10 的定义是 gold 源 span 覆盖率，你的实现是 intent 字段一致性 —— 这两个是同一个东西吗？」

**改法**（二选一，建议先做 A）：

- **A. 现在就能写（推荐）**：在简历里**显式标注口径**——
  > 「按 `expected_intent` 字段一致性判定（**代理指标，非人工 gold span**）；
  > +8.3pp 的消融对比仍只在 12 条小样本上测过。」

  然后面试时把它当**加分项**讲：
  > 「我要主动说清这个指标的性质：它的相关性判定用的是用例自带的 intent 字段，
  > 不是人工标注的 gold span。所以它能回答『同口径下我的加权有没有提升』（+8.3pp），
  > **但它不是绝对检索质量的度量**。真做 M10 需要人工标注源 span，
  > 那是我下一步要补的——这也是我为什么在仓库里把这项标成『未就绪』而不是『已达标』。」

  **主动指出自己指标的方法学缺陷，比报一个漂亮数字更让人信服。** 这是最能体现你工程成熟度的一条。

- **B. 要干活**：标 30 条人工 gold span，用一个独立脚本按 M10 定义重算，做成「代理指标 vs 硬指标」对照表。

### 必修 3：技能栏里没用过的技术要挪走（风险：中）

**简历技能栏**：`PostgreSQL、Redis、SQLite、Milvus、Elasticsearch`

项目实际用的是 **FAISS**（`faiss.IndexFlatIP`）+ SQLite（+ Redis 可选层）。
PostgreSQL 在 `docker-compose.yml` 里有服务定义，但**本机无 Docker、从未真跑过**
（台账「执行环境基线」明确记着 ❌ 本机无 docker / 无 psql）。

**面试官会问**：「Milvus 你建的什么索引？IVF_FLAT 还是 HNSW？nlist / M 取多少？」
「ES 的 mapping 怎么设计的？」→ 没跑过就穿帮。

**改法**：
- 把未真正用过的挪到「了解」档，或者干脆删掉；
- 技能栏分两档写：**熟练**（Python / FastAPI / Pydantic / FAISS / SQLite / Git / Pytest）、
  **了解**（Redis / PostgreSQL / Docker / LangGraph）；
- **宁可少写**：面试官只会深挖你写上去的东西，写得越多被挖的坑越多。

---

## 二、白丢的三个亮点（改代码零成本，只是没写）

### 亮点 1：JWT 身份体系与多租户隔离 —— **一个字都没写**

这是你**最值钱但完全没上简历**的东西。实际有的（README + 台账「破坏性变更通告」）：

- 身份**只**来自 `Authorization: Bearer <JWT>`，校验 6 个 claim（`sub` / `tenant_id` / `roles` / `iss` / `aud` / `exp`）
- 旧的自报请求头（`X-User-Role` / `X-Operator-Id`）**彻底失效**：不带令牌一律 401，带了令牌再发也不提权
- 请求体里自报的 `operator_id` / `tenant_id` **一律被忽略并覆盖**
- 缺 `RAG_JWT_SECRET` 时受保护接口 **fail closed（500，不是放行）**
- 检索侧在 **FAISS 之前**强制 `tenant` + `document_acl` + 发布状态三重前置过滤
- 审计 + 敏感文本脱敏（`mask_sensitive_text`）

**为什么这条特别值钱**：`X-User-Role 已失效` 体现的是**「不信任客户端」**这个安全直觉 ——
很多候选人会把角色放请求头图方便，你能说清为什么这是错的（客户端可伪造），
这是「懂安全」和「会用框架」的分水岭。

**可直接粘贴**：

> **JWT 身份体系与多租户隔离**：身份只来自 `Authorization: Bearer <JWT>`（六 claim 校验）；
> 旧的自报请求头与请求体自报身份一律失效并被服务端覆盖，杜绝客户端提权；
> 缺签名密钥时受保护接口 fail closed（返回 500 而非放行）；检索侧在向量库之前强制
> tenant + document_acl + 发布状态三重前置过滤，权限裁剪不依赖后置过滤。

### 亮点 2：索引版本管理、原子发布，以及一次「自我审查」

实际有的：索引以**版本 manifest** 管理、`rebuild_index` **原子发布**、
加载前**四重一致性校验**（条数 / 维度 / 文档签名）、发布与回滚都留历史与备份。

**最值钱的细节**：回滚能力曾经是 **「已建未启用」** —— 函数定义都在
（`rollback_index` / `active_index_version` / `available_versions`），
但**仓库内零调用、零测试**。后来按判据自查，在 B8-1 补齐 API 出口与 8 条测试。

**这个故事的加分点**：你不是「写了个函数就说有这能力」，而是**用判据扫出一批死代码并补上调用点**。
「定义在、测试在、没人调用 = 未完成」这个自我要求，比功能本身更稀缺。

**可直接粘贴**：

> **索引版本管理与原子发布**：索引以版本 manifest 管理，重建走原子发布
> （先构建后切换指针，避免半成品索引被读到），加载前做四重一致性校验
> （条数 / 向量维度 / 文档签名），发布与回滚均留历史与备份。
> 回滚能力曾被我自己标为「已建未启用」——函数已定义但仓库内零调用零测试，
> 按「定义在 + 测试在 + 有人调用」三条件自查后补齐 API 出口与 8 条测试。

### 亮点 3：测试规模 252 → **936**，以及「不装模型也能跑全套测试」

**过时数字**：简历写「252 个用例全绿」。实测（2026-09-24，JUnit XML 口径）：

| 项 | 数值 |
| --- | --- |
| pytest 用例总数 | **936**（`932 passed` + 4 subtests），0 失败 0 错误 |
| 发布门禁时的规模 | 918（阶段 7 结论） |
| 测试文件 | 30 个（`tests/`） |
| ruff | `All checks passed!` |
| CI 门禁 | ruff + pytest + **单文件体积守卫**（`check_repo_data_size.py`） |

**真正该写出来的是这个设计**：`requirements-dev.txt` 只有 **9 个包、不含 torch** ——
因为项目对 `torch` / `transformers` / `sentence-transformers` 都做了
`try/except ModuleNotFoundError` 降级，单测用 mock 覆盖检索与生成。
所以**别人 clone 下来 40 秒就能跑完全套测试，不用下 3.5GB 模型**。

**面试官为什么爱听**：「怎么让别人快速验证你的东西」是很多人的盲区。
你把重依赖做成可降级、把测试和模型解耦，这是**刻意的交付设计**，不是顺手做的。

---

## 三、可直接粘贴的完整版项目描述

> 替换简历里「外卖客服 RAG 智能问答系统」整段。**改动点已加粗说明，正文可直接用。**

**外卖客服 RAG 智能问答系统** | 个人项目 | **2026.02 - 至今**

**项目简介**：面向外卖售后场景的检索增强客服后端，打通「召回 → 精排 → 证据分级 → Prompt 构造 →
生成 → 规则兜底 → 可归因评测」完整链路。自建 781 条中文知识库（515 条人工种子 +
266 条京东帮助中心真实 FAQ 清洗入库），另建成 103 份文档 / 9229 chunk 的切片语料库与
chunk 级索引；**936 个用例全绿（JUnit XML 口径，0 失败 0 错误）**，ruff 通过，
**发布门禁 PASS**（含四条判据与三条限制声明）。

**技术栈**：Python、FastAPI、LangChain、FAISS、SQLite、Redis、**JWT**

**技术亮点**：

- **多信号融合打分 + cross-encoder 精排**（*原「混合召回」，术语已改准*）：外卖售后中
  「退款进度 / 商家拒绝退款 / 退款失败」的向量相似度集中在 0.72–0.83，纯向量召回会稳定混淆业务方向。
  在 FAISS 向量分之上叠加 `keyword_bonus` 与 `direction_penalty` 构成多信号打分
  （`score = vector_score + keyword_bonus − direction_penalty`），再叠 cross-encoder 精排
  （`rerank_score` 加权 0.01，权重经网格搜确定）；12 条消融集 Recall@1 由纯向量
  **0.9167 → 1.0000（+8.3pp，小样本）**。**明确未上 BM25 稀疏检索与 RRF** ——
  语料规模小、领域规则信号优于词频信号；代价是规则需手工维护、换领域要重写。

- **评测集扩容与分层归因**：发现 12 条原始评测集已饱和（Recall@1 = 1.0，无区分度）后，
  复用 grounding 用例集自带的 `expected_intent` 作检索判定依据，零额外标注把评测集扩到
  90 条固定 + 30 条盲测并按 `case_type` 分层。固定集 Recall@1 **0.9667** / MRR **0.9773** /
  NDCG@10 **0.9827**，盲测集 **0.9000 / 0.9222 / 0.9333**；分层定位最弱环节为
  `multi_intent`（0.8750）与 `inducement`（盲测 0.3333，归因后确认是安全策略改写与金标口径冲突，
  非检索 bug）。**注：相关性判定为 `expected_intent` 字段一致性，属代理指标、非人工 gold span。**
  （报告留档 `reports/retrieval_metrics/`）

- **证据分级解决模型串意图**（*原文保留*）：Top1 标为主证据、其余标为辅助证据，在 Prompt 中
  硬约束辅助证据「只能补充流程、凭证、入口，不得覆盖主证据的业务结论」；主证据与次席精排分差
  小于 0.08 时额外标记 `close_match` 提示模型谨慎引用。90 条固定集上串意图仅 3 条；
  回答返回「主证据：X」字段供前端溯源展示。

- **answer_composer 条件介入改造（A/B 量化）**（*原文保留*）：排查发现默认链路里 90/90 的
  回答都被模板无条件重组、模型贡献无法体现，改为 `on` / `off` / `auto` 三模式（`auto` 为新默认）；
  同一 judge 口径下对 90 条固定集跑三组 A/B，`judge_pass` 纯模型 **0.6444** → 条件介入 **0.7556**
  → 全规则 **0.8333**，量化出规则模板贡献 **18.9pp** 并保留 **60%** 场景的模型原文；
  三组 `forbidden` 命中均为 0，证明安全边界不依赖答案重组。

- **高风险场景规则前置拦截**（*原文保留*）：命中「食品安全 + 承诺词」「验证码 / 银行卡」
  「身份证 / 真实手机号」「站外交易」等组合时，直接返回预置拒绝话术，不经检索与生成；
  4 条诱导用例 4/4 被正确拒绝，90 + 30 条评测中 `forbidden` 命中 0。

- **JWT 身份体系与多租户隔离**（*新增，见亮点 1*）：身份只来自 `Authorization: Bearer <JWT>`
  （六 claim 校验）；旧的自报请求头与请求体自报身份一律失效并被服务端覆盖，杜绝客户端提权；
  缺签名密钥时受保护接口 fail closed（500 而非放行）；检索侧在向量库之前强制
  tenant + `document_acl` + 发布状态三重前置过滤。审计日志含敏感文本脱敏。

- **索引版本管理与原子发布**（*新增，见亮点 2*）：索引以版本 manifest 管理，重建走原子发布，
  加载前四重一致性校验（条数 / 维度 / 文档签名）；回滚能力曾属「已建未启用」，
  按「定义在 + 测试在 + 有人调用」自查后补齐 API 出口与 8 条测试。

- **可归因评测闭环与工程化**：每个用例落盘 query / 召回片段 / 最终 Prompt / 回复 / judge 结果，
  自动输出四类失败归因；测试覆盖 API 契约 / 鉴权 / 降级 / 规则 / 评测脚本。
  **CI 三道门禁**（ruff + pytest + 单文件体积守卫）；**`requirements-dev.txt` 仅 9 个包、不含 torch**
  —— 对重型依赖做 `try/except` 降级，**不装模型 40 秒可跑完全套 936 条测试**。
  单次请求返回 14 步 `full_trace`（逐步耗时），据此定位瓶颈为本地 1.5B 解码而非检索，
  端到端 P50 **4220ms** / P95 **6147ms**（CPU）；并发压测未做，QPS 未实测。

---

## 四、计划做完后的升级（按性价比排序）

| 优先 | 做完的事 | 简历能新增什么 | 参考成本 |
| --- | --- | --- | --- |
| **P0** | **F1 切轨**（`chat_service.py` 从 `retrieve_rag_items()` 切到 chunk 索引） | 「问答链路直连 chunk 级索引，**9229 chunk / 103 份文档全量可检索**，双轨合并」—— 同时**消掉简历与仓库的唯一不一致**（README 里那条 `f1-track` 锚点会从红色提醒变成已合） | 半天 ~ 1 天 |
| **P0** | **前端真登录 + 部署到公网** | 简历里能放**一个可点开的链接**。面试现场直接演示，比任何数字都有说服力 | 1 ~ 2 天 |
| **P1** | **BM25 + RRF 混合检索** | 这时「**混合检索**」才是准确的词，且能报「稠密 / 稀疏 / 融合」三组对比与 RRF 的 k 值 —— 把必修 1 从「澄清边界」升级成「真的做了」 | 1 ~ 2 天 |
| **P1** | **人工 gold span 评测集（30 条）** | 「30 条人工标注 gold span，M10 Recall@5 = 0.XX（人工口径）」，并给出**代理指标 vs 硬指标**对照 —— 必修 2 彻底闭环 | 1 天 |
| **P2** | **成本账本**（含失败 / 重试 / 评测分摊） | 「逐请求成本账本，单次成功任务成本 ¥0.0X」—— 企业最爱问「花多少钱」，你有数就是稀缺 | 1 ~ 2 天 |
| **P2** | **限流 + 请求 deadline + 背压 + 死信** | 「接口限流 + 全链路 deadline + 队列背压 + 死信重试」—— 稳定性维度 | 2 ~ 3 天 |
| **P2** | **Docker compose 真跑起来**（api / worker / postgres / redis） | 「`docker compose up -d` 一键起四服务，健康检查通过，冷启动 X 秒」—— 把「编排写了但没跑过」变成「跑过」 | 1 天（需装 Docker） |
| **P3** | 压测与 P95 / P99 聚合 | 「并发压测：X QPS 下 P95 = X ms」—— 现在这项是**未实测**，问了要坦白 | 1 天 |
| **P3** | 解析质量门禁 + 三状态字段 | 「解析质量门禁 + processing / quality / publication 三状态」—— 数据治理维度 | 2 ~ 3 天 |
| **P3** | 真实 OCR 引擎 + 病毒扫描 + RPO/RTO 演练 | 企业合规维度，**投入产出比最低**。做完只是让 checklist 更满，面试不一定会问 | 数天 |

**我的建议：只做 P0 两条 + P1 两条。** 理由：
- P0 的 F1 切轨**不做不行** —— 你的仓库是公开的，README 里明写「双轨未合」。
  简历不提还好，一旦提「9229 chunk」就必须先切轨，否则是造假。
- P0 的前端上线是**投入产出比最高的一条** —— 面试时甩一个能点的链接，
  比「发布门禁 PASS」这种自证式结论有力得多。
- P1 两条把必修 1、必修 2 从「澄清边界」变成「真的做了」。
- P2 / P3 是「企业级素养」标签，属于锦上添花；**在面试里讲清「我知道差什么、为什么先不做」，
  效果接近「做了」** —— 而成本低得多。

---

## 五、面试追问风险自查表

> 用法：对着自己问一遍，答不上来的就是还没准备好。

| 简历上这句 | 面试官会问 | 你的答案必须包含 |
| --- | --- | --- |
| 多信号融合打分 | 「为什么不用 BM25？你的『混合』和业界混合检索什么区别？」 | 主动说明是单路向量 + 规则加权；BM25 未做的原因与代价；规则的领域硬编码问题 |
| 固定集 Recall@1 0.9667 | 「gold 是谁标的？怎么判断一个候选相关？」 | 明确是 `expected_intent` 字段一致性、**代理指标**；人工 gold span 是下一步 |
| +8.3pp 消融 | 「这个对比的样本量多少？统计显著吗？」 | **12 条**，小样本；只能说明链路跑通，不能说明泛化 |
| 936 个用例 | 「跑一遍多久？依赖重吗？」 | 精简依赖 ~40s 热缓存；**不装 torch 也能跑**（try/except 降级 + mock） |
| 发布门禁 PASS | 「发布门禁覆盖什么？不覆盖什么？」 | 四条判据；**三条限制**（不覆盖检索质量指标、OCR/病毒扫描/限流/压测/成本账本、检索质量无结论） |
| 端到端 P50 4220ms | 「慢在哪？怎么优化？QPS 多少？」 | `full_trace` 定位到本地 1.5B 解码；检索毫秒级；**并发压测未做** |
| 781 条知识库 | 「真实用户量多少？线上跑过吗？」 | 全部自建合成、无真实平台数据；**未上公网**；讲规划与优先级 |
| 技能栏 Milvus / ES | 「你 Milvus 用什么索引？」 | **要么删掉，要么如实说没用过** |

---

## 六、一句话电梯陈述（更新版）

> 一个外卖客服场景的 RAG 后端：781 条中文知识库（含 266 条京东帮助中心清洗入库的真实 FAQ）
> 加 103 份文档 / 9229 chunk 的切片语料库；检索侧用向量召回叠加业务方向加权再做
> cross-encoder 精排，把证据分成主证据和辅助证据防止模型串意图，高风险场景用规则硬兜底，
> 身份走 JWT 加租户级 ACL 前置过滤；评测上我用 A/B 量化了规则与模型各自的贡献
> （纯模型 0.6444 / 条件介入 0.7556 / 全规则 0.8333），936 个用例全绿且不需装模型就能验证，
> 发布门禁 PASS。**我明确知道两件事没做到**：检索的 gold span 是代理指标不是人工标注，
> 以及问答链路还挂在演示路径上、文档语料那 9229 个 chunk 暂时检索不到 —— 前者要补标注，
> 后者是下一步第一件事。
